# Knowledge Base - FTELSRCore.Shared

> Chuan: Engineering Knowledge Documentation Standard v1.0
> Ngon ngu: Tieng Viet | Code identifier giu nguyen casing goc
> Cap nhat theo commit: `89c1ce9`

---

## 1. Cach dung tai lieu nay

Day la tap tai lieu ky thuat cho thu vien dung chung `FTELSRCore.Shared` (repo `sr-core-helper`, target `net9.0`). Toan bo noi dung duoc **doc truc tiep tu source code** trong repo, gom **30 tai lieu module** (`docs/knowledge-base/*.md`, khong tinh chinh file README nay), kem so dong tham chieu de nguoi doc tu kiem chung.

> [!NOTE]
> **Ve so hieu commit trong tung file.** 8 tai lieu duoc viet dau tien (nhom `Data-SQL-*`, `Data-MongoDB-CoreMongoDB.md`, `Utilizes-CallApi*.md`) ghi `Cap nhat theo commit: 2262829`; 22 tai lieu viet sau ghi `89c1ce9` (commit hien tai cua README nay). Da doi chieu: `git diff 2262829 89c1ce9 -- FTELSRCore.Shared` **khong co thay doi nao** tren source code — hai commit tro toi cung mot trang thai code, nen so dong tham chieu trong ca hai nhom deu con hop le. Neu repo tiep tuc thay doi sau `89c1ce9`, phai doc lai source truoc khi tin so dong.

### 1.1 Doi tuong doc va cach doc

| Doi tuong | Doc gi truoc | Muc dich |
|---|---|---|
| **Developer** (dang code) | Muc `1.3 Danh muc API` cua file lien quan -> muc `2.x` cua dung API can goi | Nam signature, input hop le, gia tri tra ve o tung nhanh, side effect |
| **Developer** (dang debug) | Muc `Van de da biet` (muc cuoi cua moi file) -> muc 5 cua README | Doi chieu trieu chung voi cai bay da duoc ghi nhan |
| **Tech Lead / Reviewer** | Muc `1.1 Pham vi chuc nang` + `Van de da biet` | Danh gia rui ro truoc khi duyet PR dung cac API nay |
| **PO / BA** | Muc `1. Tong quan` + cot "Khong lam duoc" cua bang `1.1` | Hieu gioi han nang luc that cua thu vien, tranh cam ket sai voi khach hang |
| **AI / LLM agent** | README (muc 2, 3, 4) truoc, roi file chi tiet | Dinh huong dung file, dung muc; **khong** doan API |
| **NotebookLM / RAG** | Nap toan bo thu muc `docs/knowledge-base/` (30 file + README nay) | Moi file la mot don vi ngu nghia doc lap, co header nguon va commit |

### 1.2 Nguyen tac "Source Code > Documentation"

Khi XML doc / comment trong `.cs` **mau thuan** voi than ham, tai lieu nay **luon lay than ham lam su that** va ghi ro cho mau thuan do o muc `Van de da biet`. Cac vi du da duoc ghi nhan:

- XML doc noi `data null nếu lỗi` nhung `EnsureSuccessOrException` khong nem voi 4xx/5xx (`Utilizes-CallApiWithHttp.md`, van de #2 va #24; xac nhan lai doc lap tu `Utilizes-HttpClientUtilizes.md`, van de #1 - **3 tai lieu dong nhat**).
- XML doc noi `false nếu có lỗi` nhung than ham `IsCreateOneAsync` khong co `try/catch` nao (`Data-MongoDB-CoreMongoDB.md`, van de #7).
- XML doc noi `FindAll*` tra `null` khi khong tim thay nhung than ham tra `[]` (`Data-SQL-CoreSQL-TwoEntity.md`, van de #2).
- XML doc noi `ReadDbContext` "khong chiu chi phi change tracking" nhung khong co dong nao dat `QueryTrackingBehavior` (`Data-SQL-UnitOfWork-DbContexts.md`, van de #10).
- XML doc mo ta `context` la "Đối tượng DbContext" nhung kieu that la `DbConnection` (`Data-SQL-CoreSQL.md`, van de #6).
- Ten ham `AddAuthorizationPolicy` ngu y kiem tra quyen thanh cong duoc, nhung bieu thuc so sanh `char` voi `string` khien assertion **luon fail** — policy khong bao gio pass (`Infrastructure-Auth-Swagger.md`, van de #1).
- Ten ham `EnsureSuccessOrException`/`RunWithTimeoutAsync` (tra tuple `Result`) deu goi y "nem loi khi that bai"/"biet duoc co timeout hay khong", nhung `RunWithTimeoutAsync.Result` **luon `true`** bat ke co timeout hay khong (`CrossCutting-SmallUtilities.md`, van de #1).

### 1.3 Luu y danh cho AI / LLM

> [!IMPORTANT]
> **Khong duoc suy dien capability ngoai pham vi tai lieu.** Cu the:
>
> - Khong gia dinh mot API ton tai neu no khong nam trong muc `1.3 Danh muc API` cua file tuong ung. Vi du: **khong co API Delete/Remove nao** trong `CoreSQL` (ca 2 arity) — xem `Data-SQL-CoreSQL.md` van de #14.
> - Khong gia dinh co retry / transaction / audit log / phan trang chi vi ten ham goi y nhu vay. Vi du: `*SortDeletedAsync` **khong** sap xep; `ExecuteScalar` **khong** dung `ExecuteScalarAsync`; `AuditModel` **khong** sinh ban ghi audit log; `RunWithTimeoutAsync.Result` **khong** phan anh viec co timeout hay khong.
> - **Ten co chua chu "Tenant" khong dong nghia co logic multi-tenant.** Ca `CoreSQLTenant.cs` va `TenantSRKafkaSinkExtensions`/`TenantSRKafkaSinkModel` (`Infrastructure-Observability-Logging.md`, van de #1) **da duoc doc truc tiep tu source va xac nhan KHONG co truong/logic dinh danh tenant nao** — xem muc 4.6 (CC-31) va van de toan he thong #44 duoi day truoc khi gia dinh nguoc lai.
> - Khi tai lieu ghi "**khong xac dinh duoc tu source code**", phai giu nguyen ket luan do, khong duoc thay bang phong doan hay kien thuc chung ve .NET/EF Core/Polly/FluentValidation/Serilog.
> - Moi so dong (`File.cs:123`) chi dung cho trang thai code tai commit `89c1ce9` (~ `2262829`, xem ghi chu o tren). Neu repo da thay doi tiep, phai doc lai source truoc khi tra loi.
> - Code identifier (ten class, method, property, generic parameter) **giu nguyen casing goc**, khong dich sang tieng Viet, khong doi hoa/thuong.

---

## 2. Ban do module

| Tai lieu | Module | Tang kien truc | Cong nghe | So API |
|---|---|---|---|---|
| [`Utilizes-CallApiWithHttp.md`](Utilizes-CallApiWithHttp.md) | `CallApiWithHttp<TRequest, TResponse>` | Shared / Utilizes (outbound HTTP) | `HttpClient`, `System.Text.Json`, `Newtonsoft.Json` (log) | **11** public static + 1 private helper = **12** muc |
| [`Utilizes-CallApi.md`](Utilizes-CallApi.md) | `CallApi<TResponse>` | Shared / Utilizes (outbound HTTP) | `HttpClient`, `System.Text.Json`, `Newtonsoft.Json` (log) | **9** public static |
| [`Utilizes-HttpClientUtilizes.md`](Utilizes-HttpClientUtilizes.md) | `HttpClientUtilizes` / `TokenExpirationHelperUtilizes` / `HttpContentExtensionsUtilizes` / `FormatOptions` | Shared / Utilizes (ha tang dung chung cho 2 module tren) | `HttpClient`, JWT (tu parse, khong validate signature) | **16** muc |
| [`Data-MongoDB-CoreMongoDB.md`](Data-MongoDB-CoreMongoDB.md) | `CoreMongoDB<TTable>` / `ICoreMongoDB<TTable>` | Data Access - repository (MongoDB) | `MongoDB.Driver` 3.10.0, `Polly` 8.7.0 | **32** public virtual async + 1 constructor = **33** muc |
| [`Data-MongoDB-Helpers.md`](Data-MongoDB-Helpers.md) | `ConfigurationHelpers` (Mongo) / `MongoPipelineOptimizerHelper` / `MongoResiliencePolicyFactory` | Data Access - config/toi uu pipeline/resilience (MongoDB) | `MongoDB.Driver`, `Polly` 8.7.0 | **13** muc |
| [`Data-SQL-CoreSQL.md`](Data-SQL-CoreSQL.md) | `CoreSQL<TEntity, DBContextRead, DBContextWrite>` / `ICoreSQL<...>` | Data Access - repository (SQL Server) | EF Core 9.0.18, `Dapper` 2.1.79, `Polly` 8.7.0 | **22** public method tren class (`ICoreSQL` khai bao **21**) |
| [`Data-SQL-CoreSQL-TwoEntity.md`](Data-SQL-CoreSQL-TwoEntity.md) | `CoreSQL<TEntityFrom, TEntityTo, DBContextRead, DBContextWrite>` (file `CoreSQLTenant.cs`) | Data Access - repository + mapping | EF Core 9.0.18, `Dapper` 2.1.79, `Polly` 8.7.0 | **22** public method |
| [`Data-SQL-UnitOfWork-DbContexts.md`](Data-SQL-UnitOfWork-DbContexts.md) | `IUnitOfWork` / `UnitOfWork` / `WriteDbContext<TContext>` / `ReadDbContext<TContext>` | Data Access - persistence (EF Core) | EF Core 9.0.18, `MediatR` 12.4.1 | **23** muc (4 hop dong interface + 7 `UnitOfWork` + 10 `WriteDbContext` + 2 `ReadDbContext`) |
| [`Data-SQL-Dapper.md`](Data-SQL-Dapper.md) | `IDapperSQLDBContext` / `DapperSQLDBContext` / `ExecuteSQLContext<TClass>` / `ConfigurationHelpers` (SQL) | Data Access - raw SQL (duoi tang repository) | `Dapper` 2.1.79, `Microsoft.Data.SqlClient` 7.0.2 | **13** muc (6 `DapperSQLDBContext` + 6 `ExecuteSQLContext` + 1 helper) |
| [`Data-SQL-Resilience.md`](Data-SQL-Resilience.md) | `SqlResiliencePolicyFactory` / `ReadUncommittedConnectionInterceptor` | Data Access / cross-cutting (helpers) | `Polly` 8.7.0, EF Core `DbConnectionInterceptor`, `System.Diagnostics.ActivitySource` | **9** muc trong danh muc (2 public static + 3 private static + 3 private field + 1 public override); **6** muc chi tiet cap method |
| [`Abstractions-DomainPrimitives.md`](Abstractions-DomainPrimitives.md) | `Aggregate` / `IDomainEvent` / `BaseEntityMongoDB` / `IBaseEntitySQL` va cac base entity dan xuat | Domain primitives (Abstractions, dung chung MongoDB + SQL) | `MediatR.INotification` (cho `IDomainEvent`), plain C# | **12** kieu du lieu |
| [`Audits-UserAudit.md`](Audits-UserAudit.md) | `IUserAudit` / `UserAudit` / `AuditModel` / `CreatorInfo` | Cross-cutting - audit/nguoi dung hien tai (doc tu `HttpContext`) | `IHttpContextAccessor`, `ClaimsPrincipal` (ASP.NET Core) | **18** muc |
| [`CQRS-BuildingBlocks.md`](CQRS-BuildingBlocks.md) | `ICommand<T>` / `ICommandHandler<,>` / `IQuery<T>` / `IQueryHandler<,>` / `LoggingBehavior<,>` | CQRS building blocks (bao quanh MediatR) | `MediatR` 12.4.1, `System.Diagnostics.ActivitySource` | **5** muc |
| [`Caches-CoreCache.md`](Caches-CoreCache.md) | `CoreCacheExtension` / `CoreRedisCacheExtension` / `CoreCacheHelper` / `StepCache` | Infrastructure - cache (tren FusionCache + Redis) | `ZiggyCreatures.Caching.Fusion` (`IFusionCache`), `StackExchange.Redis` | **16** muc |
| [`Constants-And-Permissions.md`](Constants-And-Permissions.md) | `CommonBaseConstant`, `ClaimTypesConstant`, `SRTypePermissions`, `RoleDataConstant`, `RoleSR`, v.v. (12 file) | Cross-cutting - hang so & danh muc quyen/role | Plain C# `const`/`enum`, `Dictionary` tinh | **12** file/nhom hang so |
| [`CrossCutting-SmallUtilities.md`](CrossCutting-SmallUtilities.md) | `CustomException`, `MeasureExecutionTimeExtensions`, `RunWithTimeoutExtensions`, `CancellationTokenHelper`, `CollectionHelpers`, `ObfuscationHelpers`, `LazyResolverExtensions`, `LazyInstanceUtility<T>`, `EnvironmentExtensions` | Cross-cutting - exception/timing/lazy-DI/obfuscation (9 file) | Plain .NET (`Lazy<T>`, `Stopwatch`, XOR+Base64) | **12** muc |
| [`Enum-CustomerType.md`](Enum-CustomerType.md) | `StatusMessageHistories` / `TypeInfomationCustomerEnum` / `TypeInfomationCustomerHelpers` | Business classification (Enum + Helpers) | Plain C# `Regex` | **13** muc |
| [`Extensions-Kafka.md`](Extensions-Kafka.md) | `KafkaExtensions` (serializer/deserializer + `CollectionNameAttribute`) / `KafkaTraceContextExtensions` | Cross-cutting - Kafka serialize + W3C trace context propagation | `Confluent.Kafka`, `Newtonsoft.Json`, `System.Diagnostics` | **6** muc |
| [`Extensions-LoggerExtensions.md`](Extensions-LoggerExtensions.md) | `LoggerExtensions` / `LoggerErrorCategoriesHelper` | Cross-cutting - logging co cau truc (dung xuyen suot repo) | `Microsoft.Extensions.Logging` (`LoggerMessage.Define`) | **36** muc |
| [`Extensions-QueryProjectionBuilders.md`](Extensions-QueryProjectionBuilders.md) | `PrecateBuilderExtensions` / `ProjectToExtensions` | Cross-cutting - predicate builder + mapping/audit-stamping (dung boi `CoreMongoDB`/`CoreSQL`) | `System.Linq.Expressions`, Reflection, `MongoDB.Driver.UpdateDefinition` | **25** muc |
| [`Extensions-Validators.md`](Extensions-Validators.md) | `AbstractValidatorExtensions` / `ValidatorRequestExtensions` | Cross-cutting - validation rule tai su dung | `FluentValidation` 12.1.1 | **15** muc |
| [`Helpers-JsonConvertHelpers.md`](Helpers-JsonConvertHelpers.md) | `JSonParseHelpers` (static partial) / `ConvertHelpers` | Cross-cutting - JSON/convert helper dung chung | `System.Text.Json`, `Newtonsoft.Json` (fallback), `MongoDB.Bson` | **22** muc |
| [`Infrastructure-Auth-Swagger.md`](Infrastructure-Auth-Swagger.md) | `AuthorizationPolicyExtensions` / `JWTBearerExtensions` / `SwaggerExtensions` / `SlugifyParameterTransformerExtensions` | Infrastructure - AuthN/AuthZ + API doc + routing | `Microsoft.AspNetCore.Authorization`, `JwtBearer`, Swashbuckle, `Asp.Versioning` | **9** muc |
| [`Infrastructure-HeaderForwarding-RedisPool.md`](Infrastructure-HeaderForwarding-RedisPool.md) | `IPAddressForwardExtensions` / `UserAgentForwardExtensions` / `ConnectionPoolRedisExtensions` | Infrastructure - outbound `DelegatingHandler` + Redis connection pool | `HttpClient` (`DelegatingHandler`), `StackExchange.Redis` | **7** muc |
| [`Infrastructure-MiddleWares.md`](Infrastructure-MiddleWares.md) | `CorrelationIdMiddleWare` / `ExceptionHandlerMiddleWare` / `MeasureExecutionTimeMiddleWare` / `SerilogHandlerMiddleWare` / `ResponseFTELCoreWrapperFilter` (+ 2 helper) | Infrastructure - HTTP pipeline (middleware) + MVC result filter | ASP.NET Core middleware/`IAsyncResultFilter`, Serilog `LogContext` | **7** muc |
| [`Infrastructure-Observability-Logging.md`](Infrastructure-Observability-Logging.md) | `SRKafkaSinkExtensions` / `TenantSRKafkaSinkExtensions` / `SRLoggerConfigurationExtensions` / `SRProducerConfigExtensions` / `SRLogEventEnricherExtensions` / `SRKafkaLogFormatter` / `OpenTelemetryExtensions` / `RegisterImplementationExtensions` | Infrastructure - observability pipeline (Serilog -> Kafka, enrich, OTel, DI reflection) | Serilog, `Confluent.Kafka`, `OpenTelemetry` | **26** muc |
| [`Models-Https.md`](Models-Https.md) | `AuthModel` / `BaseApiModel` / `ErrorModel` / `HttpOptionModel` / `HttpOptionModel<T>` / `TokenResultModel` | Model/DTO - tieu thu boi tang `Utilizes` | Plain POCO/record | **7** muc |
| [`Models-Paging-HealthCheck-Responses.md`](Models-Paging-HealthCheck-Responses.md) | `CursorPayloadModel` / `PagingModel` (+ dan xuat) / `HealthCheckModel` / `GetAllEmployeeByFilterResponse` | Model/DTO - phan trang, health check, response nhan vien | Plain POCO, Base64 (cursor encode/decode) | **12** muc |
| [`Wrappers-Result.md`](Wrappers-Result.md) | `Result` / `Result<T>` / `IResult` / `IResult<T>` / `ResultFTelCoreErrorModel` / `ResultFTelCoreMetadataModel` | Wrappers - response envelope chuan cho toan he thong | Plain C# | **24** muc |
| [`Wrappers-ErrorCodes.md`](Wrappers-ErrorCodes.md) | `CatalogsErrorCodeModel` / `ErrorSourceType` / `CatalogsErrorCode.StatusMap` / `ResponseWrapperByCodeMapper` | Wrappers - danh muc ma loi + anh xa status code -> ma loi | Plain C# `Dictionary` tinh | **6** muc |

> [!NOTE]
> **Phan biet danh tinh type.** `CoreSQL.cs:16` khai bao `CoreSQL<TEntity, DBContextRead, DBContextWrite>` (3 type parameter); `CoreSQLTenant.cs:17` khai bao `CoreSQL<TEntityFrom, TEntityTo, DBContextRead, DBContextWrite>` (4 type parameter). Khac arity nen day la **hai generic type doc lap**, khong phai hai phan `partial` cua cung mot class. **Trong repo khong ton tai type nao ten `CoreSQLTenant`** — do chi la ten file. Tuong tu, **khong ton tai type nao ten "Tenant" thuc su co logic multi-tenant** trong toan repo — xem muc 4.6.

---

## 3. Chon dung component

| Neu can lam | Dung module / API | Luu y bat buoc doc truoc |
|---|---|---|
| **Goi HTTP API co body JSON** (POST/PUT/PATCH) | `CallApiWithHttp<TRequest, TResponse>`: `PostAsJSonAsync`, `PutAsJSonAsync`, `PatchAsJSonAsync`, `PostWithHeadersAsJSonAsync` | Body lay tu `option.Value`, **khong** build query string. Bat buoc kiem tra **ca** `ErrorModel.Succeeded` **va** `data != null` (van de #2, #23). `PostWithHeadersAsJSonAsync` ghi header vao `client.DefaultRequestHeaders` va **khong xoa** -> ro ri sang request khac (van de #5) |
| **Goi HTTP API khong body** (GET/DELETE) | `CallApiWithHttp<TRequest, TResponse>`: `GetAsJSonAsync`, `GetAsJSonAndHeaderAsync`, `GetAsJSonCustomHeaderAsync`, `DeleteAsJSonAsync` | Query string duoc build bang **hai co che khac nhau** -> cung mot model sinh ra hai URL khac nhau (van de #13, #28). `GetAsJSonCustomHeaderAsync` khong null-check `headers` (van de #15) |
| **Goi HTTP API khi khong co model request** | `CallApi<TResponse>` | **Canh bao lon nhat:** `PostAsJSonAsync`, `PostWithHeadersAsJSonAsync`, `PutAsJSonAsync`, `PatchAsJSonAsync` cua lop nay **gui request KHONG CO BODY** (khong set `HttpRequestMessage.Content`) du ten co `AsJSon`. Chi `PostFormDataAsJSonAsync` co body (van de #1) |
| **Upload file qua HTTP** | `CallApiWithHttp<TRequest, TResponse>`: `PostAsFileV2Async` (uu tien) hoac `PostAsFileAsync` | `PostAsFileAsync` goi `ReadAsync` **mot lan** va bo qua so byte doc duoc -> **file gui len co the bi thieu** (van de #8). `PostAsFileV2Async` ep `HttpVersion.Version10`, khong doi duoc (van de #12). Ca hai: `cancellationTokenTime` **khong** bao trum buoc doc/mo file (van de #26) |
| **Doc trang thai het han cua 1 JWT (khong goi mang)** | `TokenExpirationHelperUtilizes.IsExpiration` / `GetExpirationTime` | Chi decode `payload`, **khong validate signature/issuer/audience** — dung de UI/logic quyet dinh refresh token, khong dung de xac thuc bao mat (`Utilizes-HttpClientUtilizes.md`) |
| **Truy van MongoDB (CRUD / aggregate)** | Ke thua `CoreMongoDB<TTable>` | Hai overload `FindAllPagingAsync(QueryContext<...>)` dung `Skip(pageNumber)` — **skip theo SO TRANG**, sai phan trang (van de #1); thu tu tham so cua chung cung nguoc `(pageSize, pageNumber)` (van de #2). **Moi ham update hardcode `IsUpsert = true`**, khong tat duoc (van de #3). `IsUpdateOneAsync(filter, entity)` co the ghi `IsDeleted = false` len document -> **hoi sinh ban ghi da xoa mem** (van de #4) |
| **Toi uu / rut gon aggregation pipeline MongoDB truoc khi gui driver** | `MongoPipelineOptimizerHelper.Optimize(List<BsonDocument>)` | Chi loai bo stage vo nghia theo cau truc tinh (khong danh gia gia tri dieu kien) — **khong** dam bao ket qua truy van khong doi trong moi truong hop bien; xem `Data-MongoDB-Helpers.md` |
| **Truy van SQL qua EF Core (1 entity)** | Ke thua `CoreSQL<TEntity, DBContextRead, DBContextWrite>` | Nhom `*SortDeletedAsync` **khong sap xep** du co chu "Sort" (van de #13). **Khong co API Delete** (van de #14). **Khong co phan trang / `OrderBy` / `Count` / `Include`** (van de #15). `FindByIdAsync` khong ap filter `IsDeleted` (van de #19) |
| **Truy van SQL qua EF Core khi entity doc khac entity ghi** | Ke thua `CoreSQL<TEntityFrom, TEntityTo, DBContextRead, DBContextWrite>` (file `CoreSQLTenant.cs`) | **Khong co logic multi-tenant nao** du ten file (van de #1). `MapUsingExpression` chi bind khi **trung ten + trung kieu + thuoc tinh nguon `CanWrite`**; ket hop `DbSet.Update` (full-row update) gay **ghi de `null`/`0` len cot dang co du lieu** — rui ro mat du lieu cao nhat cua KB (van de #5, #34) |
| **Truy van SQL raw qua Dapper (co repository)** | `CoreSQL.FindOneWithScriptAsync` / `FindAllWithScriptAsync` / `FindOneWithScalarScriptAsync` | Duoc boc `_pipelineRead` (co resilience). Nhung `SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;` bi **ghep cung** vao script khi `CommandType.Text` -> **dirty read**, khong co tham so tat (van de #7). `FindAllWithScriptAsync` tra **`null`** khi script rong, trong khi cac `FindAll*` khac tra `[]` (van de #1) |
| **Truy van SQL raw truc tiep (khong qua repository)** | `IDapperSQLDBContext`: `ExecuteNonQueryAsync`, `GetOne<T>`, `GetAll<T>`, `GetOneExecute<T>` | **Khong co resilience, khong co log, khong co transaction** o tang nay (van de #1, #10, #13). `GetAll<T>` tra **`null`** (khong phai empty) khi query rong (van de #7). `GetAllExecuteAsync<T>` **trung hoan toan** hanh vi voi `GetAll<T>` (van de #9) |
| **Goi stored procedure co output parameter** | Ke thua `ExecuteSQLContext<TClass>` | `CommandType` bi hardcode `StoredProcedure` (khong doi duoc). Ten output parameter `"P_RESULT"` hardcode, khong kiem tra ton tai (van de #3). `ExecuteScalar` dung `QueryAsync` chu khong phai `ExecuteScalarAsync` -> buffer toan bo result set (van de #5). Khong doc duoc `ReturnValue` cua SP (van de #23) |
| **Ghi SQL raw (INSERT/UPDATE/DELETE)** | `CoreSQL.IsExecuteNonQueryAsync(scriptSQLQuery, context, transaction, parameters, ...)` | Day la **ngoai le duy nhat** trong `CoreSQL`: **khong** boc Polly, **khong** truyen `cancellationToken` xuong Dapper, **khong** goi `SaveChangesAsync` (nen khong dien cot audit, khong publish domain event) — `Data-SQL-CoreSQL.md` van de #4, #5; `Data-SQL-CoreSQL-TwoEntity.md` van de #7. Khong null-check `context`/`transaction` (van de #3) |
| **Can transaction (nhieu buoc ghi nguyen tu)** | `IUnitOfWork<DBContextWrite>`: `CreateTransactionAsync` -> ... -> `CommitAsync` | **Domain event duoc publish TRUOC khi commit** (`UnitOfWork.cs:74` truoc `:76`) -> handler chay tren du lieu chua commit (van de #1). `RollbackAsync` **khong** null-check `_transaction` -> `NullReferenceException` neu goi khi chua co transaction (van de #2). `IUnitOfWork<T>` **khong** ke thua `IAsyncDisposable` -> khong `await using` duoc (van de #13). `UnitOfWork` **khong co** cach truyen `AuditModel` (van de #8) |
| **Can transaction cho raw SQL** | `CoreSQL.IsExecuteNonQueryAsync` (nhan `DbConnection` + `DbTransaction` tu ngoai) | Tang Dapper thuan (`IDapperSQLDBContext`, `ExecuteSQLContext`) **khong ho tro transaction**: `CommandDefinition` khong bao gio duoc truyen `transaction` va moi lan goi mo connection rieng (`Data-SQL-Dapper.md` van de #13) |
| **Can resilience / retry cho SQL** | `SqlResiliencePolicyFactory.ConfigureReadPolicy` / `ConfigureWritePolicy` (ung dung tieu thu tu goi khi dang ky DI) | Hai ham la `static void`, **chi mutate `ResiliencePipelineBuilder`**, khong tra ve pipeline. **Trong repo nay khong co noi nao goi chung** (van de #21) -> pipeline truyen vao `CoreSQL` co retry hay khong phu thuoc hoan toan code DI ben ngoai. Hai ham **khong idempotent** (van de #25) |
| **Can resilience / retry cho MongoDB** | `MongoResiliencePolicyFactory` -> truyen `ResiliencePipeline` vao constructor `CoreMongoDB` | `CoreMongoDB` **khong tu tao policy**; khong co gi bao dam pipeline truyen vao la pipeline do factory nay tao. Buoc duyet cursor cua aggregate nam **ngoai** pipeline -> loi khi duyet cursor khong duoc retry (van de #6). Bang tham so CB/retry doc/ghi da doi chieu khop 100% voi `Data-MongoDB-Helpers.md` |
| **Can resilience / retry cho HTTP** | **Khong co trong thu vien** | Ca `CallApi<TResponse>` va `CallApiWithHttp<TRequest, TResponse>` **khong co retry / circuit breaker / fallback**. Neu tang `IHttpClientFactory` ben ngoai gan pipeline retry, `cancellationTokenTime` se **bao trum toan bo cac lan retry** (`Utilizes-CallApiWithHttp.md` van de #20) |
| **Can dirty read cho toan bo luong doc EF Core** | `ReadUncommittedConnectionInterceptor` (ung dung tieu thu tu `AddInterceptors`) | Chi override `ConnectionOpenedAsync` -> connection mo **dong bo** khong duoc set isolation level (van de #18). **Khong reset** isolation level khi dong / tra connection ve pool (van de #19). Trong repo khong co dong `AddInterceptors` nao (van de #21) |
| **Can audit log chi tiet (old/new values)** | **Khong co trong thu vien** | `WriteDbContext.DetectChangesAudit` **luon `return []`** (`WriteDbContext.cs:356`); toan bo than ham nam trong `#region NOT SUPPORT` bi comment (`:201-353`). `AuditModel` **chi** dien cac cot audit tren chinh ban ghi, va **chi** voi entity implement `IBaseEntitySQL`. `IUserAudit.GetAuditCurrentAsync` **chi** doc du lieu nguoi dung hien tai, khong lien quan audit log |
| **Can publish domain event** | Entity ke thua **`Aggregate`** (khong phai chi implement `IAggregate`) + `WriteDbContext.SaveChangesAsync` | `DispatchDomainEvents` chi thu event tu `ChangeTracker.Entries<Aggregate>()` -> entity chi implement `IAggregate` se **mat event am tham** (`Data-SQL-CoreSQL-TwoEntity.md` van de #28; `Abstractions-DomainPrimitives.md` van de #1). Khong goi `ClearDomainEvents()` -> event co the publish lai (`Data-SQL-UnitOfWork-DbContexts.md` van de #17). Mot handler nem exception lam **dut** vong publish (van de #19) |
| **Can biet nguoi dung hien tai / dung lam `AuditModel`** | `IUserAudit.GetAuditCurrentAsync` (thuong qua DI `IUserAudit`) | Chi hoat dong trong ngu canh HTTP request co `HttpContext` — ngoai HTTP (background job) moi property fallback ve "Anonymous"/"0"/`null`/`[]`/`"FTEL"` ma khong bao gio throw (`Audits-UserAudit.md`, muc 1.1 va van de #9) |
| **Can validate input voi FluentValidation** | `ValidatorRequestExtensions` (15 extension method mo rong `IRuleBuilder<T,TProperty>`) | `IsDateTime` so sanh 2 buoc bang **2 `CultureInfo` khac nhau** — bao loi sai voi moi gia tri hop le khi `CurrentCulture` khac Invariant/en-US, vi du `vi-VN` (van de #9, **Nghiem trong**). `RegexString` mac dinh `minimumLength = 0` gan nhu vo nghia (van de #1) |
| **Can cache ket qua (memory + Redis)** | `CoreCacheExtension.GetOrCreateAsync<TOut>` (qua `IFusionCache`) | Luon fail-open (log roi tra `null`/`default`), **tru khi** `cancellationToken` da bi huy **truoc** khi goi ham — nhanh nay nem thang ra caller (van de #11). Gia tri cache luon la `string` JSON, khong phai object goc |
| **Can liet ke / xoa nhieu key Redis theo pattern, hoac chay Lua script** | `CoreRedisCacheExtension` | **Khong co try/catch nao trong ca 4 method** — khac han trieu ly fail-open cua `CoreCacheExtension` (van de #6). Loi ket noi Redis se nem thang ra caller |
| **Can wrap response chuan cho controller (thanh cong/loi)** | `Result` / `Result<T>` (implement `IResult`/`IResult<T>`) | `Fail`/`Succeed` la **static factory**, khong co logic side-effect; muon dong nhat http status code phai tu anh xa qua `Wrappers-ErrorCodes.md`. `Dispatched` chi `false` trong nhom `FailSystem` |
| **Can anh xa HTTP status code + nguon loi sang ma loi chuan cua he thong** | `ResponseWrapperByCodeMapper.FromStatusCode(HttpStatusCode, ErrorSourceType)` | `StatusMap` chi co **11** entry co dinh; status code khong khop se roi vao `FromStatusCodeDefault` (fallback tinh, xem `Wrappers-ErrorCodes.md`) |
| **Can cau hinh JWT Bearer / policy phan quyen theo claim** | `JWTBearerExtensions.AddJWTBearer`, `AuthorizationPolicyExtensions.AddAuthorizationPolicy` | `AddAuthorizationPolicy` **khong bao gio pass** do loi so sanh `char` voi `string` trong `RequireAssertion` (van de #1, **Nghiem trong** — xem muc 4.6). `IdentityModelEventSource.ShowPII = true` khi Local/Development log lo PII (van de #4) |
| **Can sinh Swagger doc / route kebab-case** | `SwaggerExtensions` (`IConfigureOptions<SwaggerGenOptions>`), `SlugifyParameterTransformerExtensions` | Dieu kien nap XML comment bi dao nguoc — comment cua controller/action **hau nhu khong bao gio** duoc nap vao Swagger UI (van de #2) |
| **Can forward IP/User-Agent sang service ha tang qua `HttpClient`** | `IPAddressForwardExtensions`, `UserAgentForwardExtensions` (dang ky nhu `DelegatingHandler`) | `ConvertHelpers.GetClientIpAddress` doc thang header client gui len, **khong xac thuc nguon** (khong `KnownProxies`) -> **rui ro gia mao IP** neu khong co tang loc phia truoc (van de #1) |
| **Can pool nhieu ket noi Redis (khong dung 1 multiplexer duy nhat)** | `ConnectionPoolRedisExtensions` | Chon connection theo round-robin qua `Lazy<IConnectionMultiplexer>`; xem file rieng de biet gioi han cua co che pool nay |
| **Can bat exception toan cuc / do latency / enrich Serilog theo request** | `ExceptionHandlerMiddleWare`, `MeasureExecutionTimeMiddleWare`, `SerilogHandlerMiddleWare` | `ResponseFTELCoreWrapperFilter` (file `ResponseWrapperMidleWare.cs`) **khong phai** ASP.NET Core Middleware ma la MVC `IAsyncResultFilter`, du ten file/namespace goi y nguoc lai (`Infrastructure-MiddleWares.md`, muc "Luu y quan trong ve phan loai") |
| **Can gui log co cau truc len Kafka / cau hinh OTel** | `SRLoggerConfigurationExtensions.SRKafkaSink` (1 topic) hoac `.TenantSRKafkaSinks` (phan tuyen theo `LogEventLevel`), `OpenTelemetryExtensions.AddFTELSRTracing`/`AddFTELSRMetrics` | **`TenantSRKafkaSinks` KHONG phan tuyen theo tenant/khach hang** du ten goi vay — xem muc 4.6. `LoadFromEnvironmentVariables()` bi ghi de ngay sau boi `.SetValue(...)` mac dinh -> 2 bien moi truong security lien quan **khong bao gio co hieu luc** (van de #2) |
| **Can log co cau truc (Information/Warning/Error) xuyen suot code nghiep vu** | `LoggerExtensions` (36 extension method) | Nhom dung tham so `Exception e` qua `logger.Log(...)` ad-hoc **chi giu duoc text** (`e.Message`/`e.StackTrace`), **khong** giu duoc doi tuong `Exception` cho sink structured — khac voi nhom dung `LoggerMessage.Define` (van de #1, **Nghiem trong**, xem muc 4.6). `FailLogic` ghi o `LogLevel.Information` (van de #6, dong nhat voi CC-17) |
| **Can serialize/deserialize gia tri Kafka message, hoac lan truyen W3C trace context qua Kafka header** | `KafkaExtensions.JSonSerializer<T>`/`JSonDeserializer<T>`, `KafkaTraceContextExtensions` | Serializer dung `Newtonsoft.Json` (khong phai `System.Text.Json` nhu phan lon repo); `Deserialize` tra `default` im lang khi parse loi, khong log |
| **Can gop/phu dinh predicate LINQ, hoac stamp `Created*`/`Modified*` cho MongoDB** | `PrecateBuilderExtensions` (`And`/`Or`/`Not`/`AddIsDeleted`), `ProjectToExtensions` (`SetDataCreatedDefault`/`SetDataUpdatedDefault`/`MapUpdateDefinition`) | Day la **ha tang thuc su** dang sau nhieu canh bao cua `CoreMongoDB`/`CoreSQLTenant` (vi du `IsDeleted = false` bi ghi de qua `MapUpdateDefinition`) — xem `Extensions-QueryProjectionBuilders.md` truoc khi doi loi tren tang repository |
| **Can convert/parse chuoi, ngay, enum, so tien bang chu, che so dien thoai** | `ConvertHelpers` (trong `Helpers-JsonConvertHelpers.md`) | `UnsignViet`/`RemoveDiacritics` la 2 cach bo dau **doc lap, khong dung chung logic**; kiem tra ky truoc khi coi 2 ham nay tuong duong |
| **Can serialize/deserialize JSON an toan (khong throw)** | `JSonParseHelpers.ToJSon<T>`, `JSonTryParse<T>` | `ToJSon` co fallback Newtonsoft khi `System.Text.Json` bao `NotSupportedException`; `JSonTryParse` **khong co** fallback tuong tu — nuot loi va tra `false`/`default` |
| **Can phan loai 1 chuoi input tu do la ten/SDT/CMND/email/ma so thue...** | `TypeInfomationCustomerHelpers.TypeInfomationCustomerEnums` | Regex detector cho ten co dau tieng Viet **khong bao trum du** vung Unicode dau to hop — co the phan loai sai voi ten co dau phuc tap (van de #5) |
| **Can chay task voi timeout kieu "fire-and-forget" (khong huy task goc)** | `RunWithTimeoutExtensions.RunWithTimeoutAsync<T>` | Tuple `Result` tra ve **luon `true`** o ca 3 nhanh — **khong** dung duoc de biet task co bi timeout hay khong (van de #1, **Cao**) |

---

## 4. Canh bao xuyen suot (cross-cutting)

Day la cac cai bay trai deu tren nhieu module. Doc muc nay truoc khi doc bat ky file chi tiet nao.

### 4.1 Audit va thoi gian

| # | Canh bao | Module bi anh huong |
|---|---|---|
| CC-1 | **`AuditModel` KHONG sinh audit log.** `DetectChangesAudit` luon `return []` (`WriteDbContext.cs:356`), nen `DispatchAuditLog` luon thoat ngay. `AuditModel` **chi** dien cac cot `Created*`/`Modified*` tren chinh ban ghi. Moi phat bieu kieu "mat audit trail khi dung raw SQL" chi dung o phan **cot audit tren entity**, khong phai audit log | `CoreSQL` (2 arity), `UnitOfWork`/`WriteDbContext`, `Audits-UserAudit.md` |
| CC-2 | **`CommonBaseConstant.DateTimeUtc()` mac dinh `addHour = 7`** -> `CreatedDate`/`ModifiedDate` luu **UTC+7**, khong phai UTC, du ten ham la `DateTimeUtc`. Khong cau hinh duoc mui gio. Da doi chieu doc lap va khop tai `Data-MongoDB-CoreMongoDB.md`, `Data-SQL-UnitOfWork-DbContexts.md`, `Constants-And-Permissions.md` (van de #13), `Extensions-Validators.md` (dung lam moc so sanh cho `DateTimeGreaterThanCurrentDate`) | `CoreMongoDB`, `WriteDbContext` (nen ca `CoreSQL` 2 arity), `Extensions-Validators` |
| CC-3 | **Bat doi xung create vs update khi `audit` la `null`.** Luong create van dong dau "Anonymous"/"0"/"FTEL"; luong update **bo qua hoan toan**, khong stamp `Modified*`, khong canh bao. Cung mau hinh xuat hien o **ca hai** overload (entity va `UpdateDefinition`) cua `ProjectToExtensions.SetData*Default` | `CoreMongoDB` (`SetDataUpdatedDefault`), `WriteDbContext` (`OnBeforeSaveChanges`), `Extensions-QueryProjectionBuilders.md` (van de #10) |
| CC-4 | **Cac cot cua `IEntityFullCreatedAndModifiedBase<T>` khong bao gio duoc stamp** (`CreatedUserRegionId`/`LocationId`/`BranchId` va ban `Modified*`), du `CreatorInfo` da mang san `RegionId`/`BranchId`/`LocationId` | `WriteDbContext` -> anh huong `CoreSQL` (2 arity), `UnitOfWork`, `Abstractions-DomainPrimitives.md` |

### 4.2 Toan ven du lieu

| # | Canh bao | Module bi anh huong |
|---|---|---|
| CC-5 | **Ghi de `null`/gia tri mac dinh len cot dang co du lieu.** Mongo: `MapUpdateDefinition` khong bo qua value type khong nullable -> `$set` luon chua `IsDeleted = false`, `int = 0`, `DateTime = default`. SQL 2-entity: `MapUsingExpression` bo qua thuoc tinh khong trung ten/kieu + `DbSet.Update` full-row -> cot tuong ung bi xoa | `CoreMongoDB` (#4), `CoreSQL<TFrom,TTo,...>` (#5, #34), `Extensions-QueryProjectionBuilders.md` (nguon goc ham) |
| CC-6 | **`ChangeTracker.Clear()` chay vo dieu kien sau moi lan luu thanh cong**, ke ca tren `DBContextWrite` do caller truyen vao. Moi entity dang duoc context theo doi bi detach, thay doi chua luu bi mat | `WriteDbContext` (#7) -> `CoreSQL` (2 arity, cac overload nhan `context`), `UnitOfWork` |
| CC-7 | **Soft delete phu thuoc ten property `"IsDeleted"` hardcode dang chuoi**, khong cau hinh duoc. Entity thieu property nay -> loi **tai runtime**, khong phai loi bien dich | `CoreMongoDB` (#18), `CoreSQL` (#12), `CoreSQL<TFrom,TTo,...>` (#12), `Extensions-QueryProjectionBuilders.md` (`AddIsDeleted`) |
| CC-8 | **Khong co API Delete nao** trong tang repository SQL. Xoa cung phai qua raw SQL; xoa mem phai tu set co roi `UpdateAsync` (chiu rui ro full-row update) | `CoreSQL` (#14), `CoreSQL<TFrom,TTo,...>` (#13) |
| CC-9 | **`IEnumerable` dau vao bi enumerate nhieu lan** (`IsNullOrEmpty()` roi `foreach`/`AddRangeAsync`/`UpdateRange`). Nguon lazy khong buffer se bi thuc thi lai hoac cho ket qua khac | `CoreMongoDB` (#23), `CoreSQL` (#20), `CoreSQL<TFrom,TTo,...>` (#20), `WriteDbContext` (#24, #36) |

### 4.3 Resilience va cau hinh

| # | Canh bao | Module bi anh huong |
|---|---|---|
| CC-10 | **Repo khong wire resilience.** `SqlResiliencePolicyFactory.ConfigureReadPolicy`/`ConfigureWritePolicy` va `MongoResiliencePolicyFactory.ConfigureReadPolicy`/`ConfigureWritePolicy` khong duoc goi o dau; `ReadUncommittedConnectionInterceptor` khong duoc `AddInterceptors`; khong co dang ky DI cho `IDapperSQLDBContext`, `IUnitOfWork`, `IDbContextFactory`, `ICoreCacheExtension`/`ICoreRedisCacheExtension`. **Muc do hieu qua thuc te khong xac dinh duoc tu source code trong repo nay** | `SqlResiliencePolicyFactory` (#21), `CoreSQL` (#24, #25), `Dapper` (#18), `UnitOfWork` (#31), `Data-MongoDB-Helpers.md`, `Caches-CoreCache.md` |
| CC-11 | **Circuit breaker duoc `Add` TRUOC retry** o ca hai factory (SQL va Mongo) -> thu tu thuc thi la `CircuitBreaker -> Retry -> lenh DB`. CB chi dem **mot** outcome cho moi lan goi pipeline (sau khi retry da can), khong dem tung attempt -> `MinimumThroughput` dat cham hon nhieu so voi truc giac | `SqlResiliencePolicyFactory` (#3), `CoreMongoDB` (#43), `Data-MongoDB-Helpers.md` (bang cau hinh doi chieu khop) |
| CC-12 | **Toan bo tham so resilience la hardcode**, khong doc `IConfiguration`/`IOptions`/`appsettings`. Muon tinh chinh theo moi truong phai sua code va deploy lai | `SqlResiliencePolicyFactory` (#10), `CoreMongoDB` (`_aggregateOptions`, #27), `Data-MongoDB-Helpers.md` |
| CC-13 | **Khong co retry noi tai cho HTTP.** Polly trong repo chi dung cho SQL va MongoDB; khong tim thay `AddPolicyHandler` nao cho `HttpClient` | `CallApi<TResponse>` (#17), `CallApiWithHttp<TRequest,TResponse>` (#20) |
| CC-14 | **Constructor khong null-check tham so nao** o hau het cac lop. Loi cau hinh DI (thieu `ILogger`, thieu `ResiliencePipeline`) chi bung ra o lan goi API dau tien duoi dang `NullReferenceException` kho truy nguyen | `CoreMongoDB` (#31), `CoreSQL` (#22), `Dapper` (#11), `UnitOfWork` (#34), `SqlResiliencePolicyFactory` (#14), `ConnectionPoolRedisExtensions`, `TenantSRKafkaSinkExtensions` (co null-check rieng cho danh sach model, ngoai le) |

### 4.4 Xu ly loi va quan sat

| # | Canh bao | Module bi anh huong |
|---|---|---|
| CC-15 | **Khong co `try/catch` o tang repository / tang Dapper / `CoreRedisCacheExtension`.** Moi exception noi thang len caller va **khong duoc ghi log** tai lop do. `_logger` chi duoc dung cho guard clause | `CoreMongoDB`, `CoreSQL` (#21), `CoreSQL<TFrom,TTo,...>` (#21), toan bo 4 file Dapper, `Caches-CoreCache.md` (#6) |
| CC-16 | **Guard clause tra ve `null`/`[]`/`0`/`false` im lang**, va **khong nhat quan giua cac ham cung nhom**: `FindAllWithScriptAsync` tra `null` con `FindAllAsync` tra `[]`; `GetAll<T>` tra `null`; `FindAllWithAggregateAsync` tra `[]` khi `pipeline` la `null` ma khong log; `RunWithTimeoutAsync.Result` luon `true` du timeout hay khong | `CoreSQL` (#1), `CoreSQL<TFrom,TTo,...>` (#3), `Dapper` (#6, #7), `CoreMongoDB` (#28), `CrossCutting-SmallUtilities.md` (#1) |
| CC-17 | **`FailLogic` ghi o `LogLevel.Information`** (`LoggerExtensions.cs:179-182`, EventId 107, category `BIZ_LOGIC`) — khong phai `Warning`/`Error`. He thong dat minimum level tu `Warning` se **mat hoan toan** dau vet cua cac truong hop guard chan. Doi chieu doc lap: `Extensions-LoggerExtensions.md` (#6) xac nhan lai dung ket luan nay | `CoreSQL` (#31), `CoreSQL<TFrom,TTo,...>` (#32), `CoreMongoDB`, `Extensions-LoggerExtensions.md` |
| CC-18 | **Nguoc lai: `UnitOfWork` ghi MOI su kien binh thuong o muc `Warning`** ("Create transaction.", "Commit transaction.", "Rollback transaction."), va khong truyen doi tuong `Exception` vao log loi -> mat stack trace | `UnitOfWork` (#20, #33), `SqlResiliencePolicyFactory` (#12, #13) |
| CC-19 | **`ProjectTo` (reflection) nuot loi va log ra `Console`, khong qua `ILogger`.** Hai overload xu ly loi **khac nhau**: overload doi tuong don nem `MissingMethodException` ra caller; overload `List<T>` bat loi va **loai phan tu khoi ket qua** -> `FindAll*Async<TDto>` tra ve danh sach thieu hoac `[]` ma khong co loi nao noi len | `CoreMongoDB` (#33), `CoreSQL` (#18), `CoreSQL<TFrom,TTo,...>` (#15, #31), `Extensions-QueryProjectionBuilders.md` (nguon goc, xac nhan khop) |
| CC-20 | **Khoi `finally` khong duoc bao ve bang `try/catch`** trong ca hai lop HTTP: no doc `option.*` va serialize `option` + `result`. Bat ky exception phat sinh trong `finally` se **thay the** gia tri tra ve / exception goc | `CallApi<TResponse>` (#21), `CallApiWithHttp<TRequest,TResponse>` (#1, #21) |
| CC-21 | **Nhom `logger.Log(...)` ad-hoc voi tham so `Exception e` chi giu duoc `e.Message`/`e.StackTrace` dang `string`**, khong giu duoc doi tuong `Exception` (khong co `InnerException`, khong group duoc theo exception type trong he thong log tap trung) — khac voi nhom dung `LoggerMessage.Define` truyen dung Exception object | `LoggerExtensions` (`HttpErrorResult`, `ErrorException`, `KafkaErrorException`, `ConnectionError*` — `Extensions-LoggerExtensions.md` #1), lan toa toi moi module goi cac wrapper nay |

### 4.5 Bao mat

| # | Canh bao | Module bi anh huong |
|---|---|---|
| CC-22 | **Token va toan bo payload response bi ghi vao log muc `Information` o MOI lan goi**, ke ca khi thanh cong: `option` (chua `Token`, `Client`) duoc serialize bang `System.Text.Json`, `result` (chua `data` da deserialize) duoc serialize bang `Newtonsoft.Json`. Khong co mask, khong gioi han kich thuoc | `CallApi<TResponse>` (#5, #5b), `CallApiWithHttp<TRequest,TResponse>` (#1) |
| CC-23 | **`Authorization` chi duoc gan khi `option.Token` khac rong va KHONG BAO GIO duoc xoa/reset.** Goi voi `Token` rong tren `HttpClient` da tung mang `Authorization` -> request van gui **token cu cua luong/nguoi dung truoc** | `CallApi<TResponse>` (#6b), `CallApiWithHttp<TRequest,TResponse>` (#3, #4) |
| CC-24 | **Header tuy chinh ghi vao `client.DefaultRequestHeaders` khong duoc remove** trong `PostWithHeadersAsJSonAsync` -> ro ri sang moi request sau tren cung `HttpClient`, khong thread-safe. Doi lap voi `GetAsJSonCustomHeaderAsync` (ghi vao `HttpRequestMessage.Headers`, dung pham vi) | `CallApi<TResponse>` (#2), `CallApiWithHttp<TRequest,TResponse>` (#5) |
| CC-25 | **Raw SQL khong duoc validate ngoai kiem tra rong/trang.** `DynamicParameters` chi bao ve **phan gia tri**, khong bao ve phan cau lenh. Neu caller ghep chuoi tu input nguoi dung -> **SQL injection** truoc khi vao tang Dapper | `CoreSQL` (#2), `CoreSQL<TFrom,TTo,...>` (#10), `Dapper` (muc 6.1) |
| CC-26 | **Dirty read bi ap dat cung.** `SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;` duoc ghep cung vao moi truy van raw SQL dang `CommandType.Text`, **khong co tham so tat**. Khong phu hop nghiep vu tai chinh / doi soat | `CoreSQL` (#7), `CoreSQL<TFrom,TTo,...>` (#11), `ReadUncommittedConnectionInterceptor` |
| CC-27 | **`AddAuthorizationPolicy` khong bao gio pass** do so sanh sai kieu (`char` vs `string`) trong `RequireAssertion` -> chuc nang phan quyen theo permission-claim **coi nhu khong hoat dong** o service nao dang dung ham nay | `AuthorizationPolicyExtensions` (`Infrastructure-Auth-Swagger.md` #1) |
| CC-28 | **`GetClientIpAddress` doc thang header do client tu gui** (`Forwarded`/`X-Forwarded-For`/`X-Real-IP`), khong co `KnownProxies`/`UseForwardedHeaders` -> **co the bi gia mao IP** khi khong co tang loc phia truoc | `ConvertHelpers` (`Helpers-JsonConvertHelpers.md`), `IPAddressForwardExtensions` (`Infrastructure-HeaderForwarding-RedisPool.md` #1) |
| CC-29 | **`IdentityModelEventSource.ShowPII = true` la thuoc tinh static toan cuc**, bat khi moi truong Local/Development -> log PII (claim/token) cho **toan bo tien trinh**, khong chi request hien tai | `JWTBearerExtensions` (`Infrastructure-Auth-Swagger.md` #4) |

### 4.6 Ten goi gay hieu nham

| # | Canh bao | Module bi anh huong |
|---|---|---|
| CC-30 | `*SortDeletedAsync` **khong sap xep** (chi loc `IsDeleted`); `FirstOrDefaultAsync` khong co `OrderBy` -> ban ghi tra ve **khong xac dinh** khi nhieu dong khop | `CoreSQL` (#13), `CoreSQL<TFrom,TTo,...>` |
| CC-31 | **Hai noi ton tai ten goi chua chu "Tenant" nhung KHONG co logic multi-tenant nao**, da doc truc tiep source va xac nhan doc lap o ca hai: (1) file `CoreSQLTenant.cs` khai bao class `CoreSQL<TEntityFrom,TEntityTo,...>` (grep `enant` = 0 ket qua) — vai tro thuc te chi la mapping entity-to-entity; (2) `TenantSRKafkaSinkExtensions`/`TenantSRKafkaSinkModel` (grep `TenantId`/`TenantCode` = 0 ket qua) — co che thuc su la **phan tuyen Kafka producer theo `LogEventLevel`**, khong lien quan tenant/khach hang. **Day khong phai 2 phat hien doc lap ngau nhien trung nhau — ca 2 cung mot dang loi dat ten "gan mac cong nghe da-tenant" cho code khong co logic do**, va la canh bao quan trong nhat cho AI/LLM khi doc repo nay (xem muc 1.3) | `CoreSQL<TFrom,TTo,...>` (`Data-SQL-CoreSQL-TwoEntity.md` #1, #41 toan he thong), `TenantSRKafkaSinkExtensions` (`Infrastructure-Observability-Logging.md` #1) |
| CC-32 | `ExecuteScalar` dung `QueryAsync` chu khong phai `ExecuteScalarAsync`; `GetAllExecuteAsync` khong ep `CommandType.StoredProcedure` du ten co `...Execute...`; `ConfigurationHelpers` (SQL) **khong doc/build gia tri config nao**; `TypeConnection` la member vestigial khong duoc tham chieu o dau | `Dapper` (#2, #5, #9, #15) |
| CC-33 | `EnsureSuccessOrException` ngu y "nem exception khi khong thanh cong" nhung **chi nem khi response la `null`**; `AddAuthorizationPolicy` ngu y "kiem tra quyen dung" nhung **luon fail**; `RunWithTimeoutAsync` tra tuple co field `Result` nhung **khong phan anh** viec co timeout; `ResponseFTELCoreWrapperFilter` nam trong file/namespace `*MiddleWare*` nhung **la MVC Result Filter**, khong phai Middleware | `Utilizes-HttpClientUtilizes.md` (#1), `Infrastructure-Auth-Swagger.md` (#1), `CrossCutting-SmallUtilities.md` (#1), `Infrastructure-MiddleWares.md` |

---

## 5. Van de da biet toan he thong

Tong hop **459** van de da ghi nhan tren **30** tai lieu (8 tai lieu nen tang du lieu/HTTP tu bo tai lieu dau tien + 22 tai lieu mo rong). Bang duoi la mot tap hop cac van de **muc do Critical va High** duoc uu tien doc truoc, sap xep Critical truoc; day **khong phai** toan bo 459 van de — danh sach day du nam o muc `Van de da biet` cua tung file (xem phan bo so luong tai muc 5.1).

| # | Van de | Module | Vi tri | Muc do |
|---|---|---|---|---|
| 1 | Hai overload `FindAllPagingAsync(QueryContext<...>)` dung `Skip(pageNumber)` — **skip theo SO TRANG**, khong nhan voi `pageSize`; cac overload khac dung `Skip((pageNumber - 1) * pageSize)`. Cung mot ten ham co hai ngu nghia phan trang | `CoreMongoDB<TTable>` | `CoreMongoDB.cs:149`, `:194` | **Critical** |
| 2 | Toan bo 6 ham update hardcode **`IsUpsert = true`**, khong co cach tat. `filter` sai -> MongoDB **tao document moi** va ham tra **`true`** | `CoreMongoDB<TTable>` | `CoreMongoDB.cs:638`, `715`, `792`, `874`, `947`, `1024` | **Critical** |
| 3 | `MapUpdateDefinition` chi bo qua property `null`; value type khong nullable **luon** vao `$set` -> `IsUpdateOneAsync(filter, entity)` voi entity dien mot phan ghi `IsDeleted = false` len document, **"hoi sinh" ban ghi da xoa mem** | `CoreMongoDB<TTable>` | `ProjectToExtensions.cs:293-304`; goi tai `CoreMongoDB.cs:622`, `863`, `940` | **Critical** |
| 4 | `MapUsingExpression` chi bind khi trung ten + trung kieu + thuoc tinh nguon `CanWrite`; thuoc tinh khong thoa bi bo qua **am tham**. Ket hop `DbSet.Update` (full-row update) -> **ghi de `null`/`0` len cot dang co du lieu** | `CoreSQL<TFrom,TTo,...>` | `ProjectToExtensions.cs:157-160`; update tai `CoreSQLTenant.cs:939`, `997`, `1076`, `1144` | **Critical** |
| 5 | `MapUsingExpression` doi hoi thuoc tinh **nguon** phai `CanWrite` moi map -> moi computed property / `{ get; }` cua `TEntityFrom` bi bo qua, cot tuong ung nhan gia tri mac dinh va bi ghi de len DB | `CoreSQL<TFrom,TTo,...>` | `ProjectToExtensions.cs:157-163` | **Critical** |
| 6 | **Domain event duoc publish TRUOC khi transaction commit.** `CommitAsync` goi `SaveChangeAsync` (-> `DispatchDomainEvents`) o `:74` roi moi `CommitAsync` o `:76`. Commit that bai + rollback -> event **da phat** cho du lieu khong ton tai | `UnitOfWork<DBContextWrite>` | `UnitOfWork.cs:74-76`; `WriteDbContext.cs:88-91` | **Critical** |
| 7 | Log tracing serialize **toan bo `option`** (gom `Token`) bang `System.Text.Json` va **toan bo `result`** (payload response) bang `Newtonsoft.Json`, o muc `Information`, **moi** lan goi | `CallApiWithHttp<,>`, `CallApi<TResponse>` | `CallApiWithHttp.cs:131-133` va 10 vi tri tuong tu; `:1577`, `:1689`, ... | **Critical** (bao mat) |
| 8 | `PostAsJSonAsync`, `PostWithHeadersAsJSonAsync`, `PutAsJSonAsync`, `PatchAsJSonAsync` tao `HttpRequestMessage` **khong set `Content`** -> gui request **khong co body**, khong co `Content-Type`, du ten chua `AsJSon` | `CallApi<TResponse>` | `CallApiWithHttp.cs:1847`, `:2098`, `:2212`, `:2440` | **Critical** |
| 9 | `PostAsFileAsync` goi `ReadAsync` **mot lan** va bo qua so byte doc duoc; ep `(int)fileStream.Length` -> **noi dung file gui len co the bi thieu**; file > 2 GB tran `int` | `CallApiWithHttp<,>` | `CallApiWithHttp.cs:663`, `665-666` | **Critical** |
| 10 | `RequireAssertion` trong `AddAuthorizationPolicy` so sanh tung ky tu (`char`) cua claim `Permissions` voi ca chuoi `authorizationPolicy` (`string`) bang `value.Equals(authorizationPolicy)`. `char.Equals(object)` voi `string` luon `false` -> `Any(...)` luon `false` -> policy **khong bao gio pass**, bat ke user co claim dung hay khong | `AuthorizationPolicyExtensions` | `AuthorizationPolicyExtensions.cs:13-15` | **Critical** |
| 11 | Nhom method dung `logger.Log(...)` ad-hoc voi tham so `Exception e` (`HttpErrorResult(...,e)`, `ErrorException`, `KafkaErrorException`, `ConnectionError*`) **chi noi `e.Message`/`e.StackTrace` (string) vao template**, khong bao gio truyen doi tuong `Exception` cho sink logging structured (Application Insights, Serilog Exception enricher, ELK) -> **khong group duoc theo exception type, mat `InnerException`** | `LoggerExtensions` | `LoggerExtensions.cs:443-458`, `:625-642`, `:546-591`, `:663-799` | **Critical** |
| 12 | `IsDateTime<T>` goi `propertyName.ToString()` **khong truyen `CultureInfo`** (dung `CurrentCulture` cua thread) roi `DateTime.TryParse` lai ep `InvariantCulture` — hai buoc dung 2 culture khac nhau. Da kiem chung thuc nghiem: voi `CurrentCulture = vi-VN`/`de-DE`/`fr-FR`/`ar-SA`, rule bao loi "dinh dang khong hop le" cho **moi** gia tri `DateTime?` hop le | `ValidatorRequestExtensions` | `ValidatorRequestExtensions.cs:102`, `:123` | **Critical** |
| 13 | `ChangeTracker.Clear()` duoc goi vo dieu kien sau moi lan luu thanh cong, ke ca tren `DBContextWrite` do caller cung cap -> detach **moi** entity context dang theo doi, mat thay doi chua luu, pha vo unit-of-work nhieu buoc | `WriteDbContext<TContext>`, `CoreSQL` (2 arity) | `WriteDbContext.cs:433` (guard o `:435-438`) | **High** |
| 14 | **`AuditModel` khong sinh audit log.** `DetectChangesAudit` luon `return []`; than ham nam trong `#region NOT SUPPORT` bi comment. `DispatchAuditLog` luon thoat ngay | `WriteDbContext<TContext>`, `CoreSQL` (2 arity) | `WriteDbContext.cs:356` (dead code `:201-353`); `:373-378` | **High** |
| 15 | `EnsureSuccessOrException` **khong nem** voi 4xx/5xx (doan `EnsureSuccessStatusCode()` bi comment) -> luong van deserialize body loi thanh `TResponse`. Co the nhan `data != null` trong khi `Succeeded = false`. Xac nhan khop tren 3 tai lieu doc lap | `CallApiWithHttp<,>`, `CallApi<TResponse>`, `HttpClientUtilizes` | `HttpClientUtilizes.cs:401-416`, dac biet `:412-415` | **High** |
| 16 | `ReadAsStreamAsync` **khong nem** khi deserialize that bai — chi log `Error` va tra `default` -> co the `(data == null, Succeeded == true)` voi HTTP 200 body sai schema | `CallApiWithHttp<,>`, `CallApi<TResponse>` | `HttpClientUtilizes.cs:317-341` | **High** |
| 17 | Header ghi vao `client.DefaultRequestHeaders` **khong bao gio duoc remove**; goi lan hai cung key -> cong don gia tri hoac `InvalidOperationException` -> `Code = 500`. Khong thread-safe | `CallApi<TResponse>` (#2), `CallApiWithHttp<,>` (#5) | `CallApiWithHttp.cs:2091`; `:975-982` | **High** |
| 18 | `Authorization` chi gan khi `Token` khac rong va **khong bao gio duoc xoa** -> request "anonymous" tren `HttpClient` dung chung van gui token cu | `CallApi<TResponse>`, `CallApiWithHttp<,>` | `HttpClientUtilizes.cs:354-357`; `CallApiWithHttp.cs:1967-1971` | **High** (bao mat) |
| 19 | `ConfigHttpClient` mutate `HttpClient` dung chung moi lan goi: gan lai `BaseAddress` (-> `InvalidOperationException` sau khi client da gui request), `Add` them `Accept` (tich luy vo han), ghi de `Authorization` | `CallApi<TResponse>`, `CallApiWithHttp<,>` | `HttpClientUtilizes.cs:343-360` | **High** |
| 20 | Query string duoc build bang **hai co che khac nhau** (`ParseModelToQueryString` vs `HttpClientUtilizes.ToQueryString`): khac ve doc `JsonPropertyNameAttribute`, ve cach encode, ve `BindingFlags`, ve xu ly gia tri whitespace -> **cung mot model sinh ra hai URL khac nhau** | `CallApiWithHttp<,>` | `CallApiWithHttp.cs:1429-1463` vs `HttpClientUtilizes.cs:111-132` | **High** |
| 21 | Response khong co `Content-Type` (dien hinh `204 No Content`) bi `ResponseResult` nem `CustomException` -> bao loi sai du thao tac da thanh cong. `ResponseResult` dung **danh sach loai tru** (khong phai cho phep) nen `text/plain`/`application/xml` van bi coi la JSON hop le va am tham deserialize sai | `CallApiWithHttp<,>`, `CallApi<TResponse>`, `HttpClientUtilizes` | `HttpClientUtilizes.cs:364-372` | **High** |
| 22 | `SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;` ghep **cung** vao moi truy van raw SQL `CommandType.Text`, khong co tham so tat -> **dirty read** toan bo duong doc raw SQL | `CoreSQL` (2 arity) | `CoreSQL.cs:82-90`, `:132-140`, `:182-190`; `CoreSQLTenant.cs:92`, `144`, `194` | **High** |
| 23 | Raw SQL khong duoc validate ngoai kiem tra rong; script duoc noi suy truc tiep vao chuoi SQL -> **SQL injection** neu caller ghep input nguoi dung | `CoreSQL` (2 arity), tang Dapper | `CoreSQL.cs:75`, `:125`, `:175`, `:228`; `CoreSQLTenant.cs:82`, `134`, `184`, `237` | **High** |
| 24 | `IsExecuteNonQueryAsync` la method **duy nhat khong duoc boc Polly pipeline**, **khong** truyen `cancellationToken` xuong Dapper, va **khong** null-check `context`/`transaction` | `CoreSQL` (2 arity) | `CoreSQL.cs:217-240`, `:235`; `CoreSQLTenant.cs:244-249` | **High** |
| 25 | `FindAllWithScriptAsync` tra **`null`** khi script rong, trong khi 4 ham `FindAll*` con lai tra `[]` -> `NullReferenceException` khi caller dung chung mot cach xu ly | `CoreSQL` (2 arity) | `CoreSQL.cs:129` vs `:510`, `548`, `583`, `617` | **High** |
| 26 | `GetAll<T>` va `GetAllExecuteAsync<T>` tra **`null`** (khong phai empty collection) khi query rong; XML doc lai noi tra `Enumerable.Empty{T}` | tang Dapper | `DapperSQLDBContext.cs:110`, `:185`; doc `IDapperSQLDBContext.cs:79` | **High** |
| 27 | Tang Dapper **khong co resilience nao** (khong Polly, khong retry, khong CB) va **khong ghi log o bat ky nhanh nao**. Goi truc tiep -> loi transient (deadlock `1205`, connection broken `-1`, timeout `-2`) that bai ngay | tang Dapper | `DapperSQLDBContext.cs:28-200`; `ExecuteSQLContext.cs:33-85` | **High** |
| 28 | `ConnectionLevelSqlErrors` la tap con thuc su cua `RetryableSqlErrors`: `-2`, `1205`, `20000` **khong** co trong tap connection-level -> DB qua tai gay timeout/deadlock lien tuc van tieu ton tron bo retry ma **circuit breaker khong bao gio mo** | `SqlResiliencePolicyFactory` | `SqlResiliencePolicyFactory.cs:24-36` vs `:42-51` | **High** |
| 29 | `MinimumThroughput = 10` + `SamplingDuration = 15s` o `ConfigureWritePolicy` -> service co tan suat ghi thap hon 10 lan goi/15 giay **khong bao gio** mo circuit breaker | `SqlResiliencePolicyFactory` | `SqlResiliencePolicyFactory.cs:162`, `:164` | **High** |
| 30 | `IsConnectionLevel` khong nhan dien `TimeoutException`; `SocketException` chi duoc kiem tra tren exception ngoai cung; `UnwrapSqlException` khong duyet `AggregateException.InnerExceptions` -> nhieu loi that su khong duoc retry va khong lam mo CB | `SqlResiliencePolicyFactory` | `:251-259`, `:234`, `:258`, `:278` | **High** |
| 31 | `RollbackAsync` **khong null-check `_transaction`** -> goi trong `catch`/`finally` phong ve khi chua co transaction nem `NullReferenceException`, **che mat exception nghiep vu goc**. Mau thuan voi `CommitAsync` (noi **co** null-check tai `:89`) | `UnitOfWork<DBContextWrite>` | `UnitOfWork.cs:109-116` (cu the `:111`) | **High** |
| 32 | `CommitAsync` khi chua co transaction: `SaveChanges` **da thanh cong** (`:74`) roi `NullReferenceException` o `:76`, khong rollback duoc (guard `:89` chan), cuoi cung `throw;` -> **du lieu da persist** nhung caller nhan exception -> de retry/ghi trung | `UnitOfWork<DBContextWrite>` | `UnitOfWork.cs:70-101` | **High** |
| 33 | `ModifiedUser`/`ModifiedDate`/`ModifiedUserCode`/`ModifiedUserOrganization` **khong bao gio** duoc gan khi luu qua `UnitOfWork` hoac `SaveChangesAsync(bool, ct)` / `SaveChanges(bool)`. `UnitOfWork` **khong co** cach truyen `AuditModel` | `UnitOfWork`, `WriteDbContext` | `WriteDbContext.cs:170-173`; `UnitOfWork.cs:155` | **High** |
| 34 | `ReadDbContext<TContext>` **khong chan ghi**: khong ghi de `SaveChanges*`, khong dat `QueryTrackingBehavior.NoTracking`, khong gan interceptor -> ve ky thuat co the ghi that vao DB qua context "read", khong qua audit stamping, khong dispatch domain event | `ReadDbContext<TContext>` | `ReadDbContext.cs:12-32` (toan file) | **High** |
| 35 | `DispatchDomainEvents` chi thu event tu `ChangeTracker.Entries<Aggregate>()` (**lop truu tuong `Aggregate`**), trong khi `CoreSQL<TFrom,TTo,...>` chuyen tiep event khi `entityConvert is IAggregate` -> entity implement `IAggregate` ma khong ke thua `Aggregate` **mat su kien am tham** | `WriteDbContext`, `CoreSQL<TFrom,TTo,...>`, `Abstractions-DomainPrimitives` | `WriteDbContext.cs:421`; `CoreSQLTenant.cs:651-654` va 7 vi tri khac | **High** |
| 36 | Exception trong mot domain event handler lam **dut** vong publish — cac event con lai bi bo. Khong retry, khong outbox, khong dead-letter | `WriteDbContext<TContext>` | `WriteDbContext.cs:444-447` | **High** |
| 37 | `BulkWriteAsync` chi xet `IsAcknowledged`, `MatchedCount`, `Upserts.Count`; **bo qua `InsertedCount` va `DeletedCount`** -> lo bulk chi gom insert hoac chi gom delete tra **`false`** du da thuc thi thanh cong | `CoreMongoDB<TTable>` | `CoreMongoDB.cs:1407-1416` | **High** |
| 38 | Nhom `FindAllAsync` / `FindAllSortDeletedAsync` (Mongo) va toan bo nhom `FindAll*` (SQL) **khong co `Limit`/`OrderBy`/`Skip`/phan trang** -> nap toan bo tap ket qua vao bo nho, rui ro `OutOfMemoryException` va command timeout | `CoreMongoDB`, `CoreSQL` (2 arity) | `CoreMongoDB.cs:403-404`, `433-434`, `457-458`, `484-485`; `CoreSQL.cs:483-618` | **High** |
| 39 | Hai `Lazy<IMongoCollection<TTable>>` dung `LazyThreadSafetyMode.ExecutionAndPublication` (mac dinh) — **cache ca exception**. Loi o lan truy cap dau tien lam instance repository **hong vinh vien** | `CoreMongoDB<TTable>` | `CoreMongoDB.cs:42-48` | **High** |
| 40 | Doc va ghi di qua **hai `IMongoDatabase` khac nhau**, khong co `IClientSessionHandle` / causal consistency -> `FindOneAsync` ngay sau `IsUpdateOneAsync` **co the khong thay** du lieu vua ghi (read-your-write khong duoc bao dam) | `CoreMongoDB<TTable>` | `CoreMongoDB.cs:42-48` | **High** |
| 41 | 3 overload `FindAllWithAggregateAsync` chay tren `_dbReadContext` -> pipeline chua `$out`/`$merge` se **ghi du lieu qua ket noi doc**; class khong kiem tra noi dung pipeline | `CoreMongoDB<TTable>` | `CoreMongoDB.cs:1274`, `1318`, `1364` | **High** |
| 42 | `ProjectTo` reflection **nuot loi** va log ra `Console`, khong qua `ILogger`; hai overload xu ly loi **khac nhau** -> `FindOneAsync<TDto>` nem exception con `FindAllAsync<TDto>` tra list rong nhu the "khong co du lieu" | `CoreMongoDB`, `CoreSQL` (2 arity), `Extensions-QueryProjectionBuilders` | `ProjectToExtensions.cs:29` vs `:90-123` | **High** |
| 43 | `FailLogic` ghi o **`LogLevel.Information`** — la nguon thong tin **duy nhat** ve cac truong hop tra `default`/`null`/`0`/`false` do guard. He thong dat minimum level tu `Warning` se mat hoan toan dau vet nay | `CoreSQL` (2 arity), `CoreMongoDB`, `LoggerExtensions` | `LoggerExtensions.cs:179-182` (EventId 107, `BIZ_LOGIC`) | **High** |
| 44 | **Hai ten goi chua chu "Tenant" ma KHONG co logic multi-tenant nao**: `CoreSQLTenant.cs` (grep `enant` = 0 ket qua) va `TenantSRKafkaSinkExtensions`/`TenantSRKafkaSinkModel` (grep `TenantId`/`TenantCode` = 0 ket qua, co che thuc su la phan tuyen theo `LogEventLevel`) -> developer/AI co the bo qua viec tu loc theo tenant, hoac tuong nham day la co che multi-tenant | `CoreSQL<TFrom,TTo,...>`, `TenantSRKafkaSinkExtensions` | `CoreSQLTenant.cs` (toan file); `TenantSRKafkaSinkExtensions.cs:20-21,41,86,101,159-167` | **High** |
| 45 | `_pipelineWrite` retry bao boc `SaveChangesAsync` ma **khong co idempotency key** va **khong kiem tra** co transaction do caller dang mo hay khong -> co the insert/update trung, hoac retry ben trong mot transaction da bi abort | `CoreSQL` (2 arity) | `CoreSQL.cs:650-655` va 7 vi tri khac; `SqlResiliencePolicyFactory.cs:194-210` | **High** |
| 46 | `ReadUncommittedConnectionInterceptor` chi override `ConnectionOpenedAsync` (khong override ban dong bo) va **khong reset** isolation level khi dong / tra connection ve pool | `ReadUncommittedConnectionInterceptor` | `ReadUncommittedConnectionInterceptor.cs:23`; toan file | **High** |
| 47 | Ten output parameter `"P_RESULT"` hardcode, khong constant, khong cau hinh, khong kiem tra ton tai truoc khi doc; khong co dong nao doc `ReturnValue` cua stored procedure | `ExecuteSQLContext<TClass>` | `ExecuteSQLContext.cs:82` | **High** |
| 48 | `cancellationTokenTime` **khong bao trum** giai doan chuan bi noi dung file trong hai method upload — `CancellationTokenSource` chi duoc tao **sau** khi doc/mo file xong | `CallApiWithHttp<,>` | `CallApiWithHttp.cs:655-673` roi `702-703`; `817-834` roi `859-860` | **High** |
| 49 | `cancellationToken.ThrowIfCancellationRequested()` va viec build URL/body nam **ngoai `try`** -> pha vo hop dong "khong bao gio nem": `OperationCanceledException`, loi serialize JSON, loi reflection query string **nem thang ra caller** | `CallApiWithHttp<,>`, `CallApi<TResponse>` | `CallApiWithHttp.cs:38`, `154`, `272`, ... | **High** |
| 50 | `IsCreateOneAsync` / `IsCreateManyAsync` **luon** `return true` sau khi goi driver, khong kiem tra ket qua nao -> `bool` tra ve khong mang thong tin nghiep vu | `CoreMongoDB<TTable>` | `CoreMongoDB.cs:1112`, `:1166` | **High** |
| 51 | Vong lap duyet cursor cua aggregate nam **ngoai** `_pipelineRead` va **thieu `.ConfigureAwait(false)`** -> loi khi duyet cursor khong duoc retry, va rui ro deadlock khi co `SynchronizationContext` | `CoreMongoDB<TTable>` | `CoreMongoDB.cs:1280`, `1324`, `1370` | **High** |
| 52 | **Repo khong wire resilience/interceptor/DI**: khong co noi goi `ConfigureReadPolicy`/`ConfigureWritePolicy` (SQL hoac Mongo), khong co `AddInterceptors`, khong dang ky `IDapperSQLDBContext`/`IUnitOfWork`/`IDbContextFactory`/`ICoreCacheExtension`, khong co lop con nao ke thua `CoreSQL<,,>`/`CoreSQL<,,,>`/`ExecuteSQLContext<>` -> **hieu qua thuc te khong xac dinh duoc tu source code trong repo nay** | Toan he thong | grep toan repo (tru `.claude/worktrees`) | **High** |
| 53 | Constructor **khong null-check** tham so nao o hau het cac lop -> loi cau hinh DI chi lo ra o lan goi API dau tien duoi dang `NullReferenceException` kho truy nguyen | Toan he thong | `CoreMongoDB.cs:32-53`; `CoreSQL.cs:35-53`; `UnitOfWork.cs:7-9`; `DapperSQLDBContext.cs:14-16` | **High** |
| 54 | `ConvertHelpers.GetClientIpAddress` doc thang `Forwarded`/`X-Forwarded-For`/`X-Real-IP` tu header client gui len, khong xac thuc nguon (khong `KnownProxies`, khong `UseForwardedHeaders` nao trong repo) -> `IPAddressForwardExtensions` co the forward IP gia mao sang service ha tang, khien tang do tin nham day la IP da qua reverse proxy | `ConvertHelpers`, `IPAddressForwardExtensions` | `ConvertHelpers.cs:61-89`; `IPAddressForwardExtensions.cs:33-34` | **High** (bao mat) |
| 55 | `RunWithTimeoutAsync<T>` tra tuple `(T Data, bool Result)` nhung `Result` **luon `true`** trong ca 3 nhanh (timeout=0, timeout xay ra, hoan tat truoc timeout) -> caller khong the phan biet "hoan tat thanh cong" voi "da timeout, task van chay ngam" | `RunWithTimeoutExtensions` | `RunWithTimeoutExtentions.cs:22,31,34` | **High** |
| 56 | `CoreRedisCacheExtension` (`GetAllDataAsync`, `GetAllKeyAsync`, `ClearDataWithKeys`, `LUAAtomicCacheAsync`) hoan toan khong co `try/catch`, khac han trieu ly fail-open cua `CoreCacheExtension` -> loi Redis/Lua script nem thang ra caller, co the crash luong xu ly neu khong tu boc | `CoreRedisCacheExtension` | toan file `CoreRedisCacheExtension.cs` | **High** |
| 57 | Dieu kien nap XML comment vao Swagger bi dao nguoc: `if (!assembly.IsDynamic) { continue; }` chi cho **assembly dynamic** di tiep, nguoc voi comment "Include all project's xml comments" ngay tren -> XML doc cua controller/action **hau nhu khong bao gio** len duoc Swagger UI du file `.xml` co ton tai tren dia | `SwaggerExtensions` | `SwaggerExtensions.cs:33-39` | **High** |

### 5.1 Phan bo van de theo tai lieu

**Nhom 8 tai lieu nen tang** (data access + outbound HTTP, viet dau tien):

| Tai lieu | So van de da ghi nhan |
|---|---|
| `Data-MongoDB-CoreMongoDB.md` | 43 |
| `Data-SQL-UnitOfWork-DbContexts.md` | 36 |
| `Data-SQL-CoreSQL-TwoEntity.md` | 34 |
| `Data-SQL-CoreSQL.md` | 32 |
| `Utilizes-CallApiWithHttp.md` | 29 |
| `Data-SQL-Resilience.md` | 28 |
| `Utilizes-CallApi.md` | 25 |
| `Data-SQL-Dapper.md` | 23 |
| **Tong nhom 8 tai lieu nen tang** | **250** |

**Nhom 22 tai lieu mo rong** (viet sau, bo sung day du 30/30 module cua thu vien):

| Tai lieu | So van de da ghi nhan |
|---|---|
| `Helpers-JsonConvertHelpers.md` | 18 |
| `Constants-And-Permissions.md` | 13 |
| `Infrastructure-Observability-Logging.md` | 12 |
| `Extensions-QueryProjectionBuilders.md` | 12 |
| `Audits-UserAudit.md` | 11 |
| `Caches-CoreCache.md` | 11 |
| `Enum-CustomerType.md` | 11 |
| `CrossCutting-SmallUtilities.md` | 10 |
| `Extensions-LoggerExtensions.md` | 10 |
| `Data-MongoDB-Helpers.md` | 9 |
| `Extensions-Validators.md` | 9 |
| `Infrastructure-Auth-Swagger.md` | 9 |
| `Infrastructure-MiddleWares.md` | 9 |
| `Abstractions-DomainPrimitives.md` | 8 |
| `Extensions-Kafka.md` | 8 |
| `Infrastructure-HeaderForwarding-RedisPool.md` | 8 |
| `Models-Paging-HealthCheck-Responses.md` | 8 |
| `Utilizes-HttpClientUtilizes.md` | 8 |
| `Models-Https.md` | 7 |
| `Wrappers-Result.md` | 7 |
| `CQRS-BuildingBlocks.md` | 6 |
| `Wrappers-ErrorCodes.md` | 5 |
| **Tong nhom 22 tai lieu mo rong** | **209** |
| **Tong toan bo 30 tai lieu** | **459** |

> [!NOTE]
> So luong o day chi phan anh **so dong** trong bang `Van de da biet` cua tung file, khong phan anh muc do nghiem trong. Vi du `Extensions-LoggerExtensions.md` chi co 10 dong nhung dong #1 duoc xep **Critical** (xem bang muc 5 va CC-21) vi anh huong lan toa qua hau het cac module con lai goi `LoggerExtensions`.

---

## 6. File khong duoc tai lieu hoa rieng

Repo co 114 file `.cs` duoi `FTELSRCore.Shared/`, trong do 111 la source code do team viet va 3 la artifact sinh tu build (`obj/Debug/net9.0/*.cs`). **Tat ca 111 file source deu duoc mot trong 30 tai lieu tren tham chieu truc tiep** (da doi chieu bang grep duong dan file trong header `> Nguon:` cua tung tai lieu so voi toan bo cay `FTELSRCore.Shared/**/*.cs`, loai tru `obj/`), ngoai tru:

| File | Ly do khong co tai lieu module rieng |
|---|---|
| `FTELSRCore.Shared/GlobalUsing.cs` | Chi chua 11 dong `global using` (khong co logic/kieu du lieu nao), nen khong dat quy cach "module co API/hanh vi" cua chuan tai lieu nay. Duoc **doc va trich dan truc tiep** trong `CrossCutting-SmallUtilities.md` (muc 1, muc 2.12) va nhieu file khac (vi du `CQRS-BuildingBlocks.md`, `Data-SQL-Resilience.md`, `Data-SQL-UnitOfWork-DbContexts.md`, `Extensions-Validators.md`, `Utilizes-CallApi.md`, `Utilizes-CallApiWithHttp.md`) moi khi mot namespace can `global using` de goi khong can `using` rieng — bao gom phat hien rang `FTELSRCore.Utilizes` (namespace cua `LazyInstanceUtility<T>`) **khong** nam trong danh sach `global using` nay, khac voi phan lon namespace con lai cua repo |

Ba file trong `FTELSRCore.Shared/obj/Debug/net9.0/` (`*.AssemblyAttributes.cs`, `*.AssemblyInfo.cs`, `*.GlobalUsings.g.cs`) la **artifact sinh tu qua trinh build**, khong phai source code do team viet, nen nam ngoai pham vi tai lieu hoa cua chuan nay.
